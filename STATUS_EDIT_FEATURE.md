# ฟีเจอร์การแก้ไขสถานะรายการหวย

## ภาพรวม
ฟีเจอร์นี้ช่วยให้ผู้ใช้สามารถแก้ไขสถานะของรายการหวยจาก "รอดำเนินการ" เป็นสถานะอื่นๆ ได้ พร้อมกับระบบติดตามประวัติการเปลี่ยนแปลง

## ฟีเจอร์ที่เพิ่มเข้ามา

### 1. ปุ่มแก้ไขสถานะ
- แสดงเฉพาะในรายการที่มีสถานะ "รอดำเนินการ"
- ปุ่มสีน้ำเงินพร้อมไอคอน Edit
- เปิด Dialog สำหรับเลือกสถานะใหม่

### 2. Dialog แก้ไขสถานะ
- แสดงเลขบิลและสถานะปัจจุบัน
- Dropdown สำหรับเลือกสถานะใหม่:
  - ยืนยันแล้ว (confirmed)
  - ยกเลิก (cancelled)
- ปุ่มยืนยันและยกเลิก

### 3. ประวัติการเปลี่ยนแปลงสถานะ
- แสดงในส่วนขยายของแถว (Expanded Row)
- แสดงรายละเอียด:
  - สถานะเก่า → สถานะใหม่
  - เวลาที่เปลี่ยนแปลง
  - ผู้ใช้ที่ทำการเปลี่ยนแปลง
- โหลดข้อมูลแบบ Lazy เมื่อขยายแถว

### 4. ระบบฐานข้อมูล
- ตาราง `status_change_history` สำหรับเก็บประวัติ
- ตาราง `lottery_tickets` เพิ่มคอลัมน์ `updated_at`
- Trigger อัตโนมัติสำหรับอัปเดต `updated_at`

## การใช้งาน

### สำหรับผู้ใช้ทั่วไป
1. เข้าไปที่หน้า "รายการบิลหวย"
2. ค้นหารายการที่มีสถานะ "รอดำเนินการ"
3. คลิกปุ่ม "แก้ไขสถานะ" (สีน้ำเงิน)
4. เลือกสถานะใหม่จาก Dropdown
5. คลิก "อัปเดตสถานะ"

### สำหรับผู้ดูแลระบบ
- สามารถแก้ไขสถานะของรายการทั้งหมด
- ดูประวัติการเปลี่ยนแปลงของทุกรายการ
- ระบบจะบันทึกผู้ใช้ที่ทำการเปลี่ยนแปลง

## โครงสร้างฐานข้อมูล

### ตาราง status_change_history
```sql
CREATE TABLE status_change_history (
    id BIGSERIAL PRIMARY KEY,
    ticket_id BIGINT NOT NULL REFERENCES lottery_tickets(id),
    user_id UUID NOT NULL REFERENCES auth.users(id),
    old_status TEXT NOT NULL,
    new_status TEXT NOT NULL,
    changed_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### ตาราง lottery_tickets (อัปเดต)
```sql
ALTER TABLE lottery_tickets ADD COLUMN updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW();
```

## การตั้งค่า RLS (Row Level Security)

### สำหรับ status_change_history
- ผู้ใช้สามารถดูประวัติของตนเอง
- ผู้ดูแลระบบสามารถดูประวัติทั้งหมด
- ผู้ใช้สามารถเพิ่มประวัติของตนเอง

## การติดตั้ง

### 1. รัน Migration
```bash
# หากใช้ Supabase CLI
supabase db push

# หรือรัน SQL โดยตรงใน Supabase Dashboard
```

### 2. ตรวจสอบการทำงาน
- ทดสอบแก้ไขสถานะของรายการ "รอดำเนินการ"
- ตรวจสอบประวัติการเปลี่ยนแปลง
- ตรวจสอบสิทธิ์การเข้าถึง

## ข้อควรระวัง

1. **สิทธิ์การเข้าถึง**: ตรวจสอบ RLS policies ให้ถูกต้อง
2. **การตรวจสอบ**: ระบบจะบันทึกผู้ใช้ที่ทำการเปลี่ยนแปลงเสมอ
3. **การย้อนกลับ**: ไม่สามารถย้อนการเปลี่ยนแปลงได้โดยตรง ต้องดูประวัติและแก้ไขใหม่

## การแก้ไขปัญหา

### ปัญหาที่อาจเกิดขึ้น
1. **ไม่เห็นปุ่มแก้ไขสถานะ**
   - ตรวจสอบว่าสถานะเป็น "pending" หรือไม่
   - ตรวจสอบสิทธิ์ผู้ใช้

2. **ไม่สามารถอัปเดตสถานะได้**
   - ตรวจสอบการเชื่อมต่อฐานข้อมูล
   - ตรวจสอบ RLS policies

3. **ไม่เห็นประวัติการเปลี่ยนแปลง**
   - ตรวจสอบว่าตาราง `status_change_history` ถูกสร้างแล้ว
   - ตรวจสอบการเชื่อมต่อกับตาราง `profiles`

## การพัฒนาต่อ

### ฟีเจอร์ที่อาจเพิ่มในอนาคต
1. การแจ้งเตือนเมื่อสถานะเปลี่ยนแปลง
2. การส่งอีเมลแจ้งเตือน
3. การรายงานสถิติการเปลี่ยนแปลงสถานะ
4. การตั้งค่าสิทธิ์การแก้ไขสถานะแบบละเอียด