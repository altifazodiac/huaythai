# ระบบจัดการค่าบริหารระบบ (Management Fee System)

## 📋 **ภาพรวม**

ระบบจัดการค่าบริหารระบบเป็นระบบที่ออกแบบมาเพื่อคำนวณและเก็บค่าบริหารระบบทุก 7 วัน โดยอัตโนมัติ โดยคำนวณจากกำไร/ขาดทุนรวม × 5% = ค่าบริหารระบบ

## 🏗️ **โครงสร้างฐานข้อมูล**

### **ตารางหลัก:**

1. **`management_fee_cycles`** - เก็บข้อมูลรอบการคำนวณ (7 วัน)
   - `id` - UUID (Primary Key)
   - `cycle_start_date` - วันที่เริ่มรอบ
   - `cycle_end_date` - วันที่สิ้นสุดรอบ
   - `cycle_number` - เลขรอบที่
   - `total_profit_loss` - กำไร/ขาดทุนรวม
   - `management_fee_rate` - อัตราค่าบริหาร (5%)
   - `management_fee_amount` - จำนวนค่าบริหารระบบ
   - `total_net_amount` - ยอดสุทธิ
   - `remaining_balance` - ยอดคงเหลือ
   - `is_paid` - สถานะการชำระ
   - `paid_at` - วันที่ชำระ

2. **`management_fee_daily_records`** - เก็บข้อมูลรายวันในแต่ละรอบ
   - `id` - UUID (Primary Key)
   - `cycle_id` - รอปรอบการคำนวณ
   - `record_date` - วันที่
   - `daily_profit_loss` - กำไร/ขาดทุนรายวัน
   - `daily_net_amount` - ยอดสุทธิรายวัน
   - `daily_management_fee` - ค่าบริหารรายวัน
   - `cumulative_profit_loss` - กำไร/ขาดทุนสะสม
   - `cumulative_net_amount` - ยอดสุทธิสะสม
   - `cumulative_management_fee` - ค่าบริหารสะสม

3. **`management_fee_scheduler_settings`** - การตั้งค่าระบบอัตโนมัติ
   - `is_enabled` - เปิด/ปิดระบบอัตโนมัติ
   - `cycle_days` - จำนวนวันต่อรอบ (7 วัน)
   - `auto_create_next_cycle` - สร้างรอบถัดไปอัตโนมัติ
   - `last_cycle_created_at` - วันที่สร้างรอบล่าสุด
   - `next_cycle_date` - วันที่สร้างรอบถัดไป

## 🔧 **ฟังก์ชันหลัก**

### **1. การคำนวณรอบการคำนวณ**
```sql
calculate_management_fee_cycle(p_start_date, p_end_date)
```
- คำนวณค่าบริหารระบบจากข้อมูล lottery_tickets และ lottery_winnings
- สร้างบันทึกรายวันอัตโนมัติ
- คืนค่า cycle_id

### **2. การสร้างบันทึกรายวัน**
```sql
create_daily_management_fee_records(p_cycle_id, p_start_date, p_end_date)
```
- สร้างบันทึกข้อมูลรายวันในรอบการคำนวณ
- คำนวณยอดสะสมทุกวัน

### **3. การดึงข้อมูลสรุป**
```sql
get_management_fee_cycle_summary(p_cycle_id)
```
- ดึงข้อมูลสรุปรอบการคำนวณ
- รวมข้อมูลรายวันในรูปแบบ JSON

### **4. ระบบอัตโนมัติ**
```sql
check_and_create_management_fee_cycle()
```
- ตรวจสอบและสร้างรอบใหม่อัตโนมัติ
- ทำงานเมื่อมีการเพิ่มข้อมูล lottery_tickets ใหม่

## 🎯 **การคำนวณ**

### **สูตรการคำนวณ:**
1. **กำไร/ขาดทุนรวม** = ยอดซื้อรวม - ยอดจ่ายรางวัลรวม
2. **ค่าบริหารระบบ** = กำไร/ขาดทุนรวม × 5%
3. **ยอดสุทธิ** = กำไร/ขาดทุนรวม - คอมมิชชั่นรวม
4. **ยอดคงเหลือ** = ค่าบริหารระบบ - ยอดสุทธิ

### **ตัวอย่าง:**
- ยอดซื้อรวม: ฿100,000
- ยอดจ่ายรางวัล: ฿80,000
- คอมมิชชั่นรวม: ฿5,000
- กำไร/ขาดทุน: ฿20,000
- ค่าบริหารระบบ: ฿1,000 (20,000 × 5%)
- ยอดสุทธิ: ฿15,000 (20,000 - 5,000)
- ยอดคงเหลือ: ฿-14,000 (1,000 - 15,000)

## 🚀 **การใช้งาน**

### **1. หน้า UI หลัก**
- **URL:** `/management-fee`
- **ฟีเจอร์:**
  - แสดงสรุปรอบการคำนวณทั้งหมด
  - สร้างรอบใหม่ด้วยตนเอง
  - มาร์ครอบเป็นชำระแล้ว
  - ดูรายละเอียดรอบการคำนวณ
  - แสดงข้อมูลรายวัน

### **2. API Endpoints**
- **GET** `/api/management-fee` - ดึงข้อมูลรอบการคำนวณ
- **POST** `/api/management-fee` - จัดการรอบการคำนวณ
  - `action: 'create_cycle'` - สร้างรอบใหม่
  - `action: 'mark_paid'` - มาร์คเป็นชำระแล้ว
  - `action: 'update_settings'` - อัปเดตการตั้งค่า
  - `action: 'check_and_create'` - ตรวจสอบและสร้างรอบใหม่

### **3. Cron Job**
- **URL:** `/api/cron/management-fee`
- **การทำงาน:** ตรวจสอบและสร้างรอบใหม่อัตโนมัติ
- **การตั้งค่า:** ใช้ Bearer token สำหรับความปลอดภัย

## ⚙️ **การตั้งค่า**

### **1. ระบบอัตโนมัติ**
```javascript
// เปิด/ปิดระบบอัตโนมัติ
await fetch('/api/management-fee', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    action: 'update_settings',
    isEnabled: true,
    cycleDays: 7,
    autoCreateNextCycle: true
  })
});
```

### **2. Cron Job**
```bash
# ตั้งค่า cron job ทุกวันเวลา 00:00
0 0 * * * curl -X POST https://your-domain.com/api/cron/management-fee \
  -H "Authorization: Bearer YOUR_CRON_SECRET_KEY"
```

## 📊 **การแสดงผล**

### **1. การ์ดสรุป**
- รอบทั้งหมด
- รอบที่ชำระแล้ว
- รอบที่รอชำระ
- ยอดรวมรอชำระ

### **2. ตารางรอบการคำนวณ**
- เลขรอบที่
- ช่วงเวลา
- กำไร/ขาดทุน
- ค่าบริหารระบบ
- ยอดสุทธิ
- ยอดคงเหลือ
- สถานะการชำระ

### **3. รายละเอียดรอบ**
- สรุปการคำนวณ
- ข้อมูลรอบ
- บันทึกรายวัน

## 🔒 **ความปลอดภัย**

1. **การตรวจสอบสิทธิ์:** เฉพาะ admin เท่านั้นที่เข้าถึงได้
2. **API Authentication:** ใช้ Bearer token สำหรับ cron job
3. **Data Validation:** ตรวจสอบข้อมูลก่อนบันทึก
4. **Error Handling:** จัดการข้อผิดพลาดอย่างเหมาะสม

## 📈 **การขยายระบบ**

### **1. การเพิ่มฟีเจอร์**
- การส่งแจ้งเตือนเมื่อครบรอบ
- การส่งรายงานอัตโนมัติ
- การเชื่อมต่อกับระบบการชำระเงิน
- การสร้างใบแจ้งหนี้

### **2. การปรับแต่ง**
- เปลี่ยนอัตราค่าบริหาร
- ปรับระยะเวลารอบ
- เพิ่มเงื่อนไขการคำนวณ
- ปรับแต่งการแสดงผล

## 🐛 **การแก้ไขปัญหา**

### **1. ปัญหาที่พบบ่อย**
- รอบไม่ถูกสร้างอัตโนมัติ
- การคำนวณไม่ถูกต้อง
- ข้อมูลไม่แสดงผล

### **2. การตรวจสอบ**
- ตรวจสอบการตั้งค่าระบบอัตโนมัติ
- ตรวจสอบข้อมูล lottery_tickets และ lottery_winnings
- ตรวจสอบ log ของ cron job

## 📝 **บันทึกการเปลี่ยนแปลง**

- **v1.0.0** - สร้างระบบพื้นฐาน
- **v1.1.0** - เพิ่มระบบอัตโนมัติ
- **v1.2.0** - เพิ่ม cron job
- **v1.3.0** - ปรับปรุง UI และ UX 